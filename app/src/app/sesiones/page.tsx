import { SessionsList } from "@/components/sessions/SessionsList";

export const metadata = { title: "Mis sesiones · Entrenamiento canino" };

export default function SessionsPage() {
  return <SessionsList />;
}
